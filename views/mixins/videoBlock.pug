mixin videoBlock(video = {})
  //--h1=video.title
  //--p=video.views
  .videoBlock 
    a(href=routes.videoDetail(video.id))
      video.videoBlcok__thumbnail(src=video.videoFile, controls=false)
      h4.videoBlock__title=video.title
      if video.views === 1
        h6.vidoeBlcok__views 1 view
      else
        h6.videoBlock__views #{video.views} views  
